<%- include('../partials/header') -%>

<div class="container">

	<h1>Users:</h1>
	<p>This page show stats on users' comments and blog posts. Will be fleshed out later.</p>

	<% users.forEach(function(user){ %>
		<div class="card">
			<h1><a href="/users/<%= user.username %>"><%= user.username %></a></h1>
			<p>Blogs created: <%= user.blogs.length %></p>
			<p>Comments made: <%= user.comments.length %></p>
		</div>
	<% }) %>

</div>

<%- include('../partials/footer') -%>